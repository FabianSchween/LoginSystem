import { Routes } from '@angular/router';
import { LoginWindowComponent } from './login-window/login-window.component';

export const routes: Routes = [{ path: '', component: LoginWindowComponent }];
